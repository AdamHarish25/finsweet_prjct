const Home = () => {
    const classname = {
        container: "w-screen h-screen p-10"
    };

    return (
      <div className={classname.container}>
            
      </div>  
    );
}

export default Home;