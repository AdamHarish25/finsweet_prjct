const Work = () => {
  const className = {
    container:
      "w-screen h-screen grid place-items-center text-3xl font-semibold",
  };

  return <div className={className.container}>Work</div>;
};

export default Work;
