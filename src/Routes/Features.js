
const Features = () => {
  const className = {
    container:
      "w-screen h-screen grid place-items-center text-3xl font-semibold",
  };

  return <div className={className.container}>Features</div>;
};

export default Features;
