/* eslint-disable */

const Blog = () => {

    const className = {
        container: "w-screen h-auto"
    }

    return (
        <div className={className.container}>

        </div>
    )
};

export default Blog;
